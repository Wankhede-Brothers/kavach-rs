[package]
name = "kavach-ope"
version = "0.1.0"
edition = "2024"
publish = false

# Layer B of the harness-rl design: offline off-policy evaluation over logged
# bandit_log rows. Pure estimators (IPS / Direct-Method / Doubly-Robust) + CIs;
# no DB or RPC dependency — callers pass in deserialized rows.

[dependencies]
serde = { workspace = true }

[lints]
workspace = true
