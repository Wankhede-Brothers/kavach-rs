// Test hub for the goal-compiler. Splits proofs into leaves so no file exceeds
// the nano-file ceiling: `common` (fixtures + legacy Pattern-6 + disk
// round-trip) and `patterns` (the five new harness patterns).
