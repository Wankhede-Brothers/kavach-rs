# Build artifacts
target/
*.rlib

# kavach-nlm: standalone NLM helper subsystem — kept local, not pushed (no other crate depends on it)
/crates/kavach-nlm/

# Local SurrealDB query/REPL scratch — not source
/history.txt

# Editor / merge backups
*.orig
*.bak

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Cargo lock is committed for binaries
# Cargo.lock

# Vendored migration tooling (SurrealDB 2.6.x export binary) — not source
/tools/

# ML embedding model cache (fastembed-rs downloads at runtime — not source)
.fastembed_cache/
**/.fastembed_cache/

# Staged sidecar CLI for `dx bundle` external_bin (kavach deploy --bundle).
# Build artifact: kavach-<target-triple> copied here before bundling.
/crates/kavach-app/bin/

# Runtime deploy mutex (kavach deploy) — machine-local transient lock, not source.
/.deploy.lock

# Local session audit/workflow scratch under .claude — transient agent output,
# not project source (the durable record lives in the kavach DB, not the repo).
/.claude/audit-digest.json
/.claude/silent-failure-audit-result.json
/.claude/wf-*.js
