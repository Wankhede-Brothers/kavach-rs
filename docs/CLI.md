# kavach CLI reference

_Generated by `kavach commands --markdown` — do not edit by hand._

## `kavach`

Kavach harness — hooks, SurrealDB kanban, SDLC phases, agent loop control

## `kavach ask`

Ask the advisor (Haiku executor + Opus advisor)

| Flag | Description | Default |
|---|---|---|
| `<prompt>` | Prompt to send to the advisor |  |
| `--max-uses` | Maximum advisor consultations (default 3) | 3 |

## `kavach bg`

Bg-mode: declare a CC 2.1.152+ /bg background-session intent keyed to a roadmap unit. SOURCE: roadmap.unit.kavach-bg-session · code.claude.com/docs/en/changelog 2.1.152

```
EXAMPLES:
  kavach bg start --project P --task roadmap.unit.foo [--isolation none|worktree]
  kavach bg status --project P
  kavach bg stop --project P --task roadmap.unit.foo
```

## `kavach bg start`

Declare a background-session intent. Prints `/bg <task>` for the agent to paste into a CC session — kavach records the bg row in kavach-db so subsequent turns can detect "bg in flight" and yield (stop-gate already short-circuits on `input.background_tasks`)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--task` | Roadmap key the bg session will work on (e.g. roadmap.unit.<x>) |  |
| `--isolation` | Worktree isolation: "none" (default — shared FS), "worktree" (separate copy). SOURCE: code.claude.com/docs/en/changelog 2.1.152 `worktree.bgIsolation` | none |

## `kavach bg status`

List active bg sessions for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose in-flight bg rows to list |  |

## `kavach bg stop`

Clear a bg-session row (after task completion or manual abort)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--task` | Roadmap key to clear (must match `bg start --task`) |  |

## `kavach bulk`

Bulk-mode: one [RCA] binds N edits in a mechanical sweep. SOURCE: roadmap.unit.kavach-bulk-mode

```
EXAMPLES:
  kavach bulk start --sweep-id lint-unused --project P --rca-file docs/rca.md \
    --scope-glob 'src/**/*.rs' --lint-class clippy --fix-strategy auto --blast-estimate 40
  kavach bulk status --project P
  kavach bulk close --sweep-id lint-unused [--reason closed|expired]
```

## `kavach bulk close`

Close a manifest. --reason {closed|expired}

| Flag | Description | Default |
|---|---|---|
| `--sweep-id` | Sweep id to close (from `bulk start` or `bulk status`) |  |
| `--reason` | Close reason: closed (success) or expired (TTL) | closed |

## `kavach bulk start`

Open a manifest binding N edits to ONE shared [RCA]

| Flag | Description | Default |
|---|---|---|
| `--sweep-id` | Unique sweep id (reuse blocked while manifest is open) |  |
| `--project` | Project slug the sweep applies to |  |
| `--rca-file` | Path to the RCA markdown file (absolute or repo-relative) |  |
| `--scope-glob` | Glob of files in scope (e.g. `src/**/*.rs`) |  |
| `--lint-class` | Lint or defect class being swept (e.g. clippy, `dead_code`) |  |
| `--fix-strategy` | Fix strategy label (e.g. auto, manual-review) |  |
| `--blast-estimate` | Estimated number of files touched (blast-radius hint for gates) |  |
| `--ttl-seconds` | Manifest TTL in seconds (default 3600); auto-expires if not closed | 3600 |
| `--approved-by` | Who approved the sweep (audit field, default \"user\") | user |

## `kavach bulk status`

List active manifests for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose open manifests to list |  |

## `kavach commands`

Print the FULL command tree (every command → subcommand → leaf) or a complete Markdown reference

| Flag | Description | Default |
|---|---|---|
| `--tree` | Render the indented command tree (default when neither flag is given) |  |
| `--markdown` | Render the complete Markdown reference (commands, flags, defaults, examples) |  |

```
EXAMPLES:
  kavach commands                 # indented tree of every path + summary
  kavach commands --tree          # same (explicit)
  kavach commands --markdown      # full reference: flags, defaults, examples
  kavach commands --markdown > docs/CLI.md

WHEN: discover the whole surface at once, or (re)generate docs/CLI.md. Walks the live clap tree — never drifts.
```

## `kavach context`

Unified JSON snapshot: kanban counts, session, phase, loop state

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--limit` | Max kanban items (default: 10) | 10 |
| `--status` | Filter kanban by status |  |
| `--key` | Filter kanban by key substring |  |

```
EXAMPLES:
  kavach context --project nicole-carpenter --limit 10
  kavach context --project P --status in_progress --key backend

WHEN: Prefer over piping stop hooks when you only need backlog visibility.
```

## `kavach db`

Manage the SurrealDB-backed memory store (projects, kanban, decisions, graph)

```
EXAMPLES:
  kavach db kanban --project nicole-carpenter --limit 10
  kavach db get --project P --category roadmap --key K --full
  kavach db write --project P --category roadmap --key K --title T --new

WHEN: All kanban/roadmap truth lives here. Prefer `db kanban` over RPC for health checks.
```

## `kavach db archive`

Archive events with no graph anchors to active roadmap (relevance-based, audit-preserving)

| Flag | Description | Default |
|---|---|---|
| `--floor-days` | Floor age in days — events younger than this are never archived | 30 |
| `--dry-run` | Dry-run: report what would be archived without modifying state |  |

```
EXAMPLES:
  kavach db archive --floor-days 30 --dry-run
  kavach db archive --floor-days 30

WHEN: Relevance-archive anchor-less events while preserving audit trail.
```

## `kavach db backfill-relationships`

Backfill typed inter-entry edges (`depends_on/blocks/supersedes/references/mentions`) by re-extracting frontmatter + wikilinks from every existing memory row. Safe to re-run; UPSERT-based

| Flag | Description | Default |
|---|---|---|
| `--project` | Optional project slug filter (default: all projects) |  |
| `--dry-run` | Print planned edges without writing |  |

```
EXAMPLES:
  kavach db backfill-relationships --project P --dry-run
  kavach db backfill-relationships

WHEN: Rebuild typed inter-entry edges from frontmatter + wikilinks. Safe to re-run.
```

## `kavach db bridge-concepts-for`

List every concept that any L1 entity in this project bridges to

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

```
EXAMPLES:
  kavach db bridge-concepts-for --project P

WHEN: List every concept this project's entities bridge to.
```

## `kavach db bridge-create`

Bridge a project-scoped L1 entity to a global L0 concept

| Flag | Description | Default |
|---|---|---|
| `--src-table` | Source table (roadmap \| decision \| research \| pattern \| `app_spec`) |  |
| `--src-key` | Source `entry_key` |  |
| `--edge` | Bridge edge (implements \| discusses \| `references_concept` \| violates) |  |
| `--concept` | Target concept name |  |

```
EXAMPLES:
  kavach db bridge-create --src-table decision --src-key foo --edge references_concept --concept paseto_v4

WHEN: Link a project-scoped L1 row to a global L0 concept.
```

## `kavach db bridge-projects-for`

List every project (via L1 entity) that bridges to this concept

| Flag | Description | Default |
|---|---|---|
| `--concept` | Concept name |  |

```
EXAMPLES:
  kavach db bridge-projects-for --concept paseto_v4

WHEN: List every project that bridges to a given concept.
```

## `kavach db citation-add`

Add or refresh a citation (official-docs context) keyed by (project, entry-key)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--entry-key` | Stable entry key (`snake_case`) |  |
| `--name` | Display name (e.g. `SurrealDB`) |  |
| `--slug` | One metadata slug (e.g. "records") |  |
| `--url` | Official-docs URL (non-empty) |  |

```
EXAMPLES:
  kavach db citation-add --project P --entry-key surreal --name SurrealDB --slug records --url https://surrealdb.com/docs

WHEN: Pin an official-docs citation to a (project, entry-key).
```

## `kavach db citation-get`

Fetch one citation (bumps `access_count`)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--entry-key` | Entry key |  |

```
EXAMPLES:
  kavach db citation-get --project P --entry-key surreal

WHEN: Fetch one citation (bumps its access_count).
```

## `kavach db citation-link`

Merge a node into a citation via a `->cite->` edge (`table:key` ids)

| Flag | Description | Default |
|---|---|---|
| `--node` | Source node record id (`decision:foo`, `entity:bar`) |  |
| `--citation` | Target citation record id (`citation:baz`) |  |

```
EXAMPLES:
  kavach db citation-link --node decision:foo --citation citation:surreal

WHEN: Attach a graph node to a citation via a ->cite-> edge.
```

## `kavach db citation-list`

List every citation for a project (newest-updated first)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

```
EXAMPLES:
  kavach db citation-list --project P

WHEN: List a project's citations, newest-updated first.
```

## `kavach db citation-refresh`

Flow RLAIF reward along a citation's `cite` edges (bumps edge weight)

| Flag | Description | Default |
|---|---|---|
| `--citation` | Citation record id (`citation:baz`) |  |
| `--delta` | Reward delta (negative = penalty) | 1 |

```
EXAMPLES:
  kavach db citation-refresh --citation citation:surreal --delta 1.0

WHEN: Flow RLAIF reward along a citation's cite edges.
```

## `kavach db citation-traverse`

List the nodes that cite a citation (single `<-cite` walk)

| Flag | Description | Default |
|---|---|---|
| `--citation` | Citation record id (`citation:baz`) |  |

```
EXAMPLES:
  kavach db citation-traverse --citation citation:surreal

WHEN: List every node that cites a given citation.
```

## `kavach db concept-add`

Insert-or-update a global L0 concept (cross-project knowledge graph node)

| Flag | Description | Default |
|---|---|---|
| `--name` | Canonical concept id (`snake_case`, e.g. `paseto_v4`) |  |
| `--display` | Display label (e.g. "PASETO v4") |  |
| `--desc` | Description / prose body |  |
| `--tags` | Comma-separated tags (e.g. token,crypto,stateless) |  |
| `--sources` | Comma-separated source URLs |  |

```
EXAMPLES:
  kavach db concept-add --name paseto_v4 --display 'PASETO v4' --desc '...' --tags token,crypto --sources https://...

WHEN: Add/update a global L0 concept node in the cross-project graph.
```

## `kavach db concept-delete`

Remove a single concept row by canonical name

| Flag | Description | Default |
|---|---|---|
| `--name` | Concept name (`snake_case`) |  |

```
EXAMPLES:
  kavach db concept-delete --name paseto_v4

WHEN: Remove one concept row by canonical name.
```

## `kavach db concept-delete-prefix`

Bulk-purge concept rows by name prefix (requires --confirm)

| Flag | Description | Default |
|---|---|---|
| `--prefix` | Name prefix (e.g. "keyword:" to purge harvest noise) |  |
| `--confirm` | Required to actually run the bulk purge |  |

```
EXAMPLES:
  kavach db concept-delete-prefix --prefix keyword: --confirm

WHEN: Bulk-purge harvest-noise concept rows by name prefix.
```

## `kavach db concept-link`

RELATE two concepts via an ontology edge

| Flag | Description | Default |
|---|---|---|
| `--from` | Source concept name |  |
| `--edge` | Ontology edge (`is_a`, `part_of`, `prerequisite_of`, `alternative_to`, composes, mitigates, `instance_of`, subsumes) |  |
| `--to` | Target concept name |  |

```
EXAMPLES:
  kavach db concept-link --from paseto_v4 --edge alternative_to --to jwt

WHEN: Relate two concepts via an ontology edge.
```

## `kavach db concept-list`

List every concept

| Flag | Description | Default |
|---|---|---|
| `--limit` | Max results | 50 |

```
EXAMPLES:
  kavach db concept-list --limit 50

WHEN: Enumerate every concept node.
```

## `kavach db concept-search`

Full-text search concepts by description (BM25)

| Flag | Description | Default |
|---|---|---|
| `--query` | Search terms |  |
| `--limit` | Max results | 20 |

```
EXAMPLES:
  kavach db concept-search --query 'stateless token' --limit 20

WHEN: Full-text (BM25) lookup of concepts by description.
```

## `kavach db delete`

Delete specific record(s) by key or category (granular, preferred over wipe-project)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--key` | Specific key to delete (omit with --all for category-wide) |  |
| `--all` | Delete all records in category (requires --confirm) |  |
| `--confirm` | Confirm deletion |  |
| `--dry-run` | Dry-run: show what would be deleted |  |

```
EXAMPLES:
  kavach db delete --project P --category roadmap --key K --confirm
  kavach db delete --project P --category research --all --dry-run

WHEN: Granular removal of one row or a whole category. Prefer over wipe-project.
```

## `kavach db delete-prefix`

Bulk-purge records in a category whose key starts with a prefix (requires --confirm). E.g. clear every `heal.incident.loophole-*` roadmap card in one pass

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--prefix` | Key prefix to match (e.g. "heal.incident.loophole-") |  |
| `--confirm` | Confirm the bulk purge (skipped under --dry-run) |  |
| `--dry-run` | Dry-run: count what would be deleted without deleting |  |

```
EXAMPLES:
  kavach db delete-prefix --project P --category roadmap --prefix heal.incident.loophole- --dry-run
  kavach db delete-prefix --project P --category roadmap --prefix heal.incident.loophole- --confirm

WHEN: Bulk-purge a key-prefix family (e.g. stale heal-incident cards) in one pass.
```

## `kavach db event`

Append an event to the log

| Flag | Description | Default |
|---|---|---|
| `--type` | Event type (e.g. "`file_write`", "`session_start`") |  |
| `--payload` | JSON payload |  |

```
EXAMPLES:
  kavach db event --type file_write --payload '{"path":"x.rs"}'

WHEN: Append an audit event to the log (usually hook-driven).
```

## `kavach db expire`

Archive expired memory entries

```
EXAMPLES:
  kavach db expire

WHEN: Archive memory rows whose TTL has elapsed.
```

## `kavach db find-part`

Find part matching an absolute path

| Flag | Description | Default |
|---|---|---|
| `--path` | Absolute file or directory path |  |

```
EXAMPLES:
  kavach db find-part --path /abs/path/to/backend/x.rs

WHEN: Resolve which registered sub-part owns an absolute path.
```

## `kavach db find-project`

Find project matching an absolute path

| Flag | Description | Default |
|---|---|---|
| `--path` | Absolute file or directory path |  |

```
EXAMPLES:
  kavach db find-project --path /abs/path/to/file.rs

WHEN: Resolve which registered project owns an absolute path.
```

## `kavach db flow-add`

Store an implementation-flow DAG (structured JSON ingest, render-on-read)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the flow belongs to |  |
| `--key` | Flow key, unique per project (e.g. `auth-flow`) |  |
| `--title` | Display title |  |
| `--steps-json` | Path to JSON `{steps:[...],edges:[...]}` (reads stdin if omitted) |  |
| `--mermaid` | Optional raw Mermaid source cached for round-trip |  |

```
EXAMPLES:
  kavach db flow-add --project P --key build-flow --title 'Build Flow' --steps-json steps.json
  echo '{"steps":[{"id":"a","label":"compile"},{"id":"b","label":"test"}],"edges":[{"from":"a","to":"b"}]}' | kavach db flow-add --project P --key ci --title CI

The DAG renders back to a Mermaid `flowchart TD` via `kavach db flow-show`; project flows are injected into session-start context as [FLOW] Mermaid for implementation-order awareness.

WHEN: Capture how an implementation HAS to be ordered, so future sessions recall the plan as a graph.
```

## `kavach db flow-show`

Render a stored implementation-flow DAG as Mermaid (default) or JSON

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Flow key |  |
| `--format` | Output format: `mermaid` (default) or `json` | mermaid |

```
EXAMPLES:
  kavach db flow-show --project P --key build-flow           # Mermaid flowchart TD
  kavach db flow-show --project P --key build-flow --format json

WHEN: Recall the intended implementation order; paste the Mermaid into any renderer to view the graph.
```

## `kavach db gate-config-delete`

Delete a gate-config override, reverting to the compiled default

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug (`*` for the global row) |  |
| `--gate-key` | Gate-config key |  |

```
EXAMPLES:
  kavach db gate-config-delete --project P --gate-key dup.block

WHEN: Revert a gate override back to its compiled default.
```

## `kavach db gate-config-get`

Resolve a gate-config override (project-then-global), or report the miss

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug (`*` for the global row) |  |
| `--gate-key` | Gate-config key (e.g. `dup.block`, `session.autonomy_contract`) |  |

```
EXAMPLES:
  kavach db gate-config-get --project P --gate-key dup.block
  kavach db gate-config-get --project '*' --gate-key dup.block   # global

WHEN: Resolve a gate override (project then global), or confirm the miss.
```

## `kavach db gate-config-list`

List every gate-config override for a project (`*` for the globals)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

```
EXAMPLES:
  kavach db gate-config-list --project P
  kavach db gate-config-list --project '*'    # globals

WHEN: See every gate override for a project (or the global row).
```

## `kavach db gate-config-set`

Set a gate-config override (exactly one value flag per `--kind`)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug (`*` for the global row) |  |
| `--gate-key` | Gate-config key |  |
| `--kind` | Value kind: `threshold` \| `pattern_list` \| `enabled` \| `severity` \| `text` |  |
| `--num` | Numeric value (for `--kind threshold`) |  |
| `--boolean` | Boolean value (for `--kind enabled`) |  |
| `--list` | Comma-separated list (for `--kind pattern_list`) |  |
| `--text` | Text value (for `--kind severity` \| `--kind text`) |  |

```
EXAMPLES:
  kavach db gate-config-set --project P --gate-key dup.block --kind enabled --boolean true
  kavach db gate-config-set --project '*' --gate-key dup.threshold --kind threshold --num 0.85

WHEN: Override one gate's config; pass exactly one value flag matching --kind.
```

## `kavach db get`

Fetch a single memory entry by key

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--key` | Entry key |  |
| `--full` | Show all metadata (tags, ttl, source, timestamps) |  |
| `--snippet` | Return the SHORT snippet body (single-key get returns FULL content by default — naming one exact key is a depth request) |  |

```
EXAMPLES:
  kavach db get --project P --category roadmap --key roadmap.unit.foo --full

WHEN: Before implementing — always `--full` for roadmap units.
```

## `kavach db graph-query`

Query the knowledge graph — list entities and their edges

| Flag | Description | Default |
|---|---|---|
| `--entity-type` | Filter by entity type (skill, rule, gate, project, file, memory, session, `event_type`) |  |
| `--name` | Show edges for a specific entity name |  |
| `--limit` | Max entities to show (default: 20) | 20 |

```
EXAMPLES:
  kavach db graph-query --entity-type concept --limit 20
  kavach db graph-query --name paseto_v4

WHEN: Inspect graph entities and their edges by type or name.
```

## `kavach db infer-deps`

Infer `depends_on` edges from card-key sequence tokens (tier backfill)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--apply` | Write the inferred edges (default is a dry run that only prints them) |  |

```
EXAMPLES:
  kavach db infer-deps --project kavach-rs            # dry run — review proposed edges
  kavach db infer-deps --project kavach-rs --apply    # write the edges

WHEN: The DAG tier view collapses every card into TIER 0 because no card declares prerequisites.
```

## `kavach db kanban`

Show kanban board for a project (todo / `in_progress` / done)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--limit` | Max items to show (default: 15, 0 = unlimited) | 15 |
| `--status` | Filter by status (todo, `in_progress`, done) — open lanes only; verified/planned rows are terminal/backlog and not shown here unless --include-verified is passed |  |
| `--active-first` | Show `in_progress` items first |  |
| `--key` | Filter by key substring (e.g. "backend" matches "backend.crate.foo") |  |
| `--lane` | Filter by dispatch lane (exact match). Shows only cards pinned to this lane — the board lens for affinity-sharded sessions |  |
| `--include-verified` | Also render terminal `verified` rows in a [VERIFIED] lens. Off by default — the board shows OPEN work; this surfaces closed items (e.g. to confirm a unit reached `verified`, not just `done`) |  |
| `--json` | Output as JSON for agent parsing |  |
| `--format` | Override the always-on dependency-DAG view |  |

```
EXAMPLES:
  kavach db kanban --project nicole-carpenter --limit 10
  kavach db kanban --project P --status in_progress --key backend --json

WHEN: Session start and after every card close — prefer over stop-hook pipes.
```

## `kavach db kanban-close`

Close a kanban roadmap item by key (marks status=verified)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Roadmap entry key to close (e.g. phase-1-auth) |  |

```
EXAMPLES:
  kavach db kanban-close --project P --key roadmap.unit.foo

WHEN: Mark a roadmap card verified once `kavach verify` has passed.
```

## `kavach db lane-set`

Pin a roadmap card to a dispatch LANE (or clear it back to unlaned). A session running `KAVACH_LANE=<name>` dispatches its own lane first, then the unlaned backlog, never a foreign lane. Refuses if the row is absent (no implicit insert). Roadmap only

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Entry key (must already exist) |  |
| `--lane` | Lane name to pin the card to. Mutually exclusive with --clear |  |
| `--clear` | Clear the lane (back to the unlaned general backlog). Mutually exclusive with --lane |  |

```
EXAMPLES:
  kavach db lane-set --project P --key K --lane backend
  kavach db lane-set --project P --key K --clear

WHEN: Shard a roadmap card to a named dispatch lane (or return it to the general backlog).
```

## `kavach db list-parts`

List parts for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

```
EXAMPLES:
  kavach db list-parts --project my-app

WHEN: Enumerate the registered sub-parts of one project.
```

## `kavach db list-projects`

List all registered projects

```
EXAMPLES:
  kavach db list-projects

WHEN: See every registered project slug + root path.
```

## `kavach db mistake-hit-count`

Count inbound `instance_of` edges on an `anti_pattern` (mistake recurrence)

| Flag | Description | Default |
|---|---|---|
| `--name` | Anti-pattern canonical name (e.g. `anti.self_imposed_limit.abc12345`) |  |

```
EXAMPLES:
  kavach db mistake-hit-count --name anti.self_imposed_limit.abc12345

WHEN: See how many times an anti-pattern has recurred (inbound instance_of edges).
```

## `kavach db mistake-purge`

Purge an `anti_pattern` cluster + its `mistake_event`s by the gate that recorded them — removes a stale `correct_action` from `PRACTICE_DELTA` (requires `--confirm`)

| Flag | Description | Default |
|---|---|---|
| `--gate` | Gate whose `anti_pattern` cluster(s) to delete (e.g. `capture_finding_unpersisted`) |  |
| `--confirm` | Confirm the destructive purge |  |

```
EXAMPLES:
  kavach db mistake-purge --gate capture_finding_unpersisted --confirm

WHEN: Remove a stale anti-pattern cluster recorded by a gate (clears it from PRACTICE_DELTA).
```

## `kavach db next-prompt`

Print the top-priority todo card's exec_prompt to stdout (pipe to an executor model)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

```
EXAMPLES:
  kavach db next-prompt --project P
  kavach db next-prompt --project P | <executor-model>

WHEN: Serve the top-priority todo card's exec_prompt to a cheaper executor model.
```

## `kavach db ope-audit`

Reward-hacking audit: SOFT held-out value vs HARD witness value drift + C2 floor. Read-only inspection of the Layer-P5 promotion gate

| Flag | Description | Default |
|---|---|---|
| `--limit` | Max bandit rows to load (newest first) | 500 |
| `--drift-tolerance` | Drift slack before SOFT-vs-HARD divergence counts as hacking | 0.05 |

```
EXAMPLES:
  kavach db ope-audit --limit 500 --drift-tolerance 0.05

WHEN: Reward-hacking audit — SOFT vs HARD value drift on the Layer-P5 promotion gate.
```

## `kavach db ope-evaluate`

Off-policy-evaluate a candidate policy against logged bandit rows (LCB + coverage). Read-only inspection of the Layer-B RL gate

| Flag | Description | Default |
|---|---|---|
| `--allow` | Candidate P(Allow) |  |
| `--ask` | Candidate P(Ask) |  |
| `--block` | Candidate P(Block) |  |
| `--limit` | Max bandit rows to load (newest first) | 500 |
| `--z` | z-score for the lower confidence bound (1.96 ≈ 95%) | 1.96 |
| `--min-coverage-ratio` | Coverage floor in [0,1]; below it the estimate is flagged untrustworthy | 0.2 |

```
EXAMPLES:
  kavach db ope-evaluate --allow 0.7 --ask 0.2 --block 0.1 --limit 500

WHEN: Off-policy-evaluate a candidate Allow/Ask/Block policy against logged bandit rows.
```

## `kavach db pg-drift`

Detect likely missing FKs — columns named `<table>_id` with no declared FK

| Flag | Description | Default |
|---|---|---|
| `--dsn` | Postgres DSN |  |

```
EXAMPLES:
  kavach db pg-drift --dsn postgres://localhost/mydb

WHEN: Detect `<table>_id` columns missing a declared FK.
```

## `kavach db pg-er`

Emit ER diagram in Mermaid format from the live schema

| Flag | Description | Default |
|---|---|---|
| `--dsn` | Postgres DSN |  |

```
EXAMPLES:
  kavach db pg-er --dsn postgres://localhost/mydb

WHEN: Emit a Mermaid ER diagram from the live schema.
```

## `kavach db pg-introspect`

Introspect a local `PostgreSQL` database — list tables, columns, FKs

| Flag | Description | Default |
|---|---|---|
| `--dsn` | Postgres DSN (e.g. <postgres://localhost/mydb>) |  |

```
EXAMPLES:
  kavach db pg-introspect --dsn postgres://localhost/mydb

WHEN: List a live Postgres schema's tables, columns, and FKs.
```

## `kavach db pg-isolation`

Find isolated tables (zero incoming + outgoing FK edges)

| Flag | Description | Default |
|---|---|---|
| `--dsn` | Postgres DSN |  |

```
EXAMPLES:
  kavach db pg-isolation --dsn postgres://localhost/mydb

WHEN: Find tables with no FK edges (often a modeling smell).
```

## `kavach db populate-graph`

Populate the knowledge graph from existing relational data

```
EXAMPLES:
  kavach db populate-graph

WHEN: One-time/after-import build of the knowledge graph from relational rows.
```

## `kavach db priority-set`

Set or clear the priority of an existing entry without touching title/content. Surgical rerank verb for human-in-loop focus shifts. Refuses if the row does not exist (no implicit insert). Lower number = higher rank, like nice(1)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (roadmap or decision — only these carry priority) |  |
| `--key` | Entry key (must already exist) |  |
| `--priority` | New priority (lower = higher rank). Mutually exclusive with --clear |  |
| `--clear` | Clear the priority (back to NONE, FIFO tail). Mutually exclusive with --priority |  |

```
EXAMPLES:
  kavach db priority-set --project P --category roadmap --key K --priority 1
  kavach db priority-set --project P --category roadmap --key K --clear

WHEN: Re-rank the backlog by hand without touching a card's title or content.
```

## `kavach db query`

Query memory entries for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--all` | Include DONE items (filtered out by default for roadmap) |  |
| `--depth` | Per-row content depth: a char count (e.g. `--depth 400`) or `all` for the whole body. Omitted prints titles only (breadth). `KAVACH_NO_TRUNCATE=1` forces `all` everywhere |  |

```
EXAMPLES:
  kavach db query --project P --category roadmap
  kavach db query --project P --category decision --depth 400 --all

WHEN: Browse a category's rows; add --depth for body text, --all to include done items.
```

## `kavach db query-raw`

Run a read-only `SurrealQL` query against the store (`SELECT`/`INFO` only). Ad-hoc graph inspection; mutations go through typed verbs (write, mistake-purge, …)

| Flag | Description | Default |
|---|---|---|
| `--query` | The read-only `SurrealQL` to execute, e.g. `SELECT name FROM entity LIMIT 5` |  |

```
EXAMPLES:
  kavach db query-raw --query 'SELECT name FROM entity LIMIT 5'

WHEN: Ad-hoc read-only SurrealQL (SELECT/INFO only); mutations go through typed verbs.
```

## `kavach db register`

Register a project with absolute path

| Flag | Description | Default |
|---|---|---|
| `--slug` | Project slug (unique identifier) |  |
| `--path` | Absolute path to project root |  |
| `--stack` | Tech stack (e.g. "rust\|axum") |  |

```
EXAMPLES:
  kavach db register --slug my-app --path /abs/path --stack 'rust|axum'

WHEN: First-time onboarding of a repo so kavach can bind sessions to it.
```

## `kavach db register-part`

Register a sub-part within a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Parent project slug |  |
| `--name` | Part name (e.g. "backend", "frontend") |  |
| `--path` | Absolute path to part root |  |
| `--type` | Part type (backend, frontend, database, mobile, infra, docs, shared, other) |  |

```
EXAMPLES:
  kavach db register-part --project my-app --name backend --path /abs/backend --type backend

WHEN: A monorepo has distinct parts (backend/frontend/infra) you want scoped separately.
```

## `kavach db rotate`

Delete events older than N days (TIME-BASED — transitional, prefer `archive`)

| Flag | Description | Default |
|---|---|---|
| `--days` | Number of days to retain |  |

```
EXAMPLES:
  kavach db rotate --days 30

WHEN: Drop events older than N days. Prefer `archive` (relevance-based) over this.
```

## `kavach db run-record`

Record a harness execution run row (status lifecycle start)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--entry-key` | Roadmap entry key this run executes |  |
| `--branch` | Git branch the run targets |  |
| `--status` | Initial status (e.g. `running`) |  |
| `--pid` | OS process id, when known |  |

```
EXAMPLES:
  kavach db run-record --project P --entry-key roadmap.unit.foo --status running --pid 12345

WHEN: Open a harness-execution run row at start of a dispatched task.
```

## `kavach db run-update-status`

Update a run row's terminal status + exit code

| Flag | Description | Default |
|---|---|---|
| `--id` | Run row id |  |
| `--status` | New status (e.g. `done`, `failed`) |  |
| `--exit-code` | Process exit code, when terminal |  |

```
EXAMPLES:
  kavach db run-update-status --id run:abc --status done --exit-code 0

WHEN: Close a run row with its terminal status + exit code.
```

## `kavach db search`

Search with metadata filters (`entry_status`, since, contains) SOURCE: <https://docs.rs/clap/latest/clap>/_derive/_tutorial/index.html

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--status` | Filter by `entry_status` (planned, todo, `in_progress`, done, verified) |  |
| `--since` | Filter entries updated within duration (e.g. 7d, 30d, 1h) |  |
| `--contains` | Filter by title/content containing substring |  |
| `--limit` | Max results (default: 20) | 20 |

```
EXAMPLES:
  kavach db search --project P --category roadmap --status in_progress
  kavach db search --project P --since 7d --contains scylla --limit 50

WHEN: Narrow a category by status/recency/substring instead of listing everything.
```

## `kavach db set-parent`

Set (or clear) a project's parent, building the hierarchy

| Flag | Description | Default |
|---|---|---|
| `--child` | Child project slug |  |
| `--parent` | Parent project slug; omit to detach to top-level |  |

```
EXAMPLES:
  kavach db set-parent --child sub-app --parent mono
  kavach db set-parent --child sub-app          # detach to top-level

WHEN: Build or unbuild the project hierarchy.
```

## `kavach db status-update`

Update the strict status of a memory entry (`todo|in_progress|done|verified`)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--key` | Entry key |  |
| `--status` | New status: planned, todo, `in_progress`, done, verified |  |

```
EXAMPLES:
  kavach db status-update --project P --category roadmap --key K --status in_progress
  kavach db status-update --project P --category roadmap --key K --status done

WHEN: Claim card (todo→in_progress), finish work (→done), then run `kavach verify` (→verified).
```

## `kavach db sync`

Sync session state to database

```
EXAMPLES:
  kavach db sync

WHEN: Flush in-memory session state to the store after a batch of changes.
```

## `kavach db tree`

Render the project hierarchy as an indented tree

```
EXAMPLES:
  kavach db tree

WHEN: View the whole project hierarchy as an indented tree.
```

## `kavach db wipe-project`

Wipe ALL data for a project (DANGEROUS — use `delete` for granular removal)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug to wipe |  |
| `--confirm` | Confirm destructive operation (DANGEROUS: deletes ALL project data) |  |
| `--dry-run` | Dry-run: show what would be deleted without deleting |  |

```
EXAMPLES:
  kavach db wipe-project --project P --dry-run
  kavach db wipe-project --project P --confirm

WHEN: DANGEROUS — destroy ALL data for a project. Prefer `delete` for anything granular.
```

## `kavach db write`

Write a memory entry. Strict mode (`roadmap/decision/research/pattern/app_spec)`: pass either `--new` (create — fuzzy-checks against existing titles) or `--update-key <existing>` (update a known row). Plain `--key` without either flag is rejected to prevent stale duplicate rows. SOURCE: <https://docs.rs/clap/latest/clap>/_derive/_tutorial/index.html

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--category` | Category (decision, research, roadmap, pattern, app_spec) |  |
| `--key` | Entry key (unique within project+category) |  |
| `--title` | Entry title |  |
| `--content` | Content. If omitted, the body is read from a piped/redirected stdin (e.g. `... < plan.md` or `cat plan.md \| ...`); an interactive terminal with no pipe stores the title only |  |
| `--new` | Intent: create a brand-new row. Gate fuzzy-matches title against existing rows in same category; refuses if similarity >= 0.85. Mutually exclusive with --update-key |  |
| `--update-key` | Intent: update an existing row by its key. CLI verifies the key exists in same project+category; refuses if not found. Mutually exclusive with --new |  |
| `--priority` | Dispatch priority (roadmap/decision only). Lower number = higher rank — `--priority 1` is picked before `--priority 2`, like nice(1). Omit to leave existing value unchanged on update or NONE on insert (NONE rows sort after every prioritized row, then by `created_at` ASC) |  |
| `--depends-on` | Declarative dependency edge(s): this card `depends_on` the given key(s). Repeatable (`--depends-on a --depends-on b`). A bare key resolves to the same project+category; a `slug/cat/key` qname is used verbatim. Feeds the DAG scheduler's topological ordering so dependents wait until each target reaches done/verified — the precise complement to the NLU prose scanner and frontmatter |  |
| `--exec-prompt` | Opus-authored executor prompt (roadmap only), served verbatim by `db next-prompt` |  |

```
EXAMPLES:
  kavach db write --project P --category roadmap --key K --title T --new
  kavach db write --project P --category decision --key K --title T --update-key K < body.md

WHEN: Persist a decision/roadmap/research row. Always pass --new (create) or --update-key (edit).
```

## `kavach deploy`

One-shot deploy: build, test, install binary to ~/.local/bin/kavach

| Flag | Description | Default |
|---|---|---|
| `--skip-tests` | Skip the cargo nextest step (build + install only) |  |

```
EXAMPLES:
  just install              # from kavach-rs (plain CLI)
  kavach deploy --skip-tests

WHEN: After engine/cli changes; restarts RPC daemon on success.
```

## `kavach doctor`

Self-audit kavach's OWN source for silent-failure / unproven-DELETE patterns (read-only)

```
Scans kavach-engine/session/rpc/surreal. Exit 1 if findings. Add `// doctor:ok` to silence a reviewed benign line.
```

## `kavach gates`

Run a gate hook (called by Claude Code / Cursor hooks)

| Flag | Description | Default |
|---|---|---|
| `<gate_name>` | Gate name (pre-write, post-write, pre-tool, post-tool, intent, stop, session-start, six-file-intent, pre-implementation, post-implementation, …) |  |
| `--hook` | Read JSON input from stdin (hook mode) |  |
| `--verify` | Verify a prompt against a gate without hook mode (dry-run) |  |
| `--vendor` | Force the harness dialect (claude-code\|cursor\|codex). Omit to auto-detect from the payload (falls back to `$KAVACH_HARNESS`, then Claude Code). Lets one hook command serve all three IDEs |  |

```
EXAMPLES:
  kavach gates stop --help          # gate purpose (no stdin)
  echo '{"hook_event_name":"Stop","cwd":"."}' | kavach gates stop --hook --vendor cursor

WHEN: IDE hooks only. For kanban health use `kavach db kanban` or `kavach context`.
```

## `kavach goal`

Goal-mode: declare a condition for CC 2.1.139+ /goal cross-turn loops. SOURCE: roadmap.unit.kavach-goal-bridge

```
EXAMPLES:
  kavach goal start --project P --condition 'kanban todo lane is empty'
  kavach goal start --project P --condition 'cargo test green' --oracle-check 'cargo nextest run -p foo'
  kavach goal status --project P
  kavach goal stop --project P --condition 'kanban todo lane is empty'
  kavach goal compile --goal-id <slug>
  kavach goal reconcile --goal-id <slug> --oracle-result pass
```

## `kavach goal compile`

Compile a goal's `.kavach/goals/<id>/loop.yaml` into a Claude Code Workflow `workflow.js` (the oracle-gated loop runner)

| Flag | Description | Default |
|---|---|---|
| `--goal-id` | Goal id slug (directory under `.kavach/goals/`) |  |

## `kavach goal reconcile`

Apply an oracle verdict to the proof-gated completion flag. The Workflow loop calls this after each oracle run: `pass` lets the goal close, anything else keeps the stop gate blocking

| Flag | Description | Default |
|---|---|---|
| `--goal-id` | Goal id slug (same as `goal compile --goal-id`) |  |
| `--oracle-result` | Oracle verdict: pass \| fail \| skip (only pass closes the goal) |  |

## `kavach goal start`

Declare a goal condition. Prints `/goal <condition>` for the agent to paste into a CC session — kavach records the goal in kavach-db for audit

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the goal belongs to |  |
| `--condition` | Natural-language done condition (also the /goal string) |  |
| `--evaluator` | Evaluator model tier for goal checks (default haiku) | haiku |
| `--roadmap-key` | Optional roadmap key the goal closes (e.g. roadmap.unit.strict-lint-zero) |  |
| `--oracle-check` | Optional oracle command whose exit code 0 proves the goal is done. When set, emits `.kavach/goals/<slug>/loop.yaml` (oracle-gated loop) |  |

## `kavach goal status`

List active goals for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose active goals to list |  |

## `kavach goal stop`

Stop tracking the goal (emits `/goal clear` for the agent)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--condition` | Condition string matching the row to stop (from `goal status`) |  |

## `kavach heal`

Self-healing pipeline (Kavach replaces N8N). `capture` gathers CI/bug-hunt failure context and writes a self-heal roadmap card the loop will dispatch

```
EXAMPLES:
  kavach heal capture --project P --incident run-42 --summary 'smoke test failed' --log ci.log

WHEN: a CI run failed or a bug-hunt found a defect — enqueue it for the autonomous loop to fix. Kavach never calls an LLM; the subscription agent heals.
```

## `kavach heal capture`

Gather a failure's context (logs, changed files) and write its self-heal roadmap card. Idempotent on `--incident` (re-capture updates one card)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the card belongs to |  |
| `--incident` | Stable incident id (CI run id / bug-hunt finding id) — the card key |  |
| `--summary` | One-line failure summary (the card title) |  |
| `--log` | Path to the build/test log to tail into the card (optional) |  |
| `--diff-base` | Git ref to diff against for changed files (default: HEAD~1) | HEAD~1 |

## `kavach heal ingest`

Ingestion bridge (host-side): poll OPEN GitHub Issues labelled `self-heal` (opened by the CI self-heal workflow), capture each as a local roadmap card via the RPC single-writer path, then relabel the issue `self-heal-queued` so it is ingested exactly once. Connects the runner issue queue (H2) to the local card (H1). Kavach never calls an LLM

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the captured cards belong to |  |

## `kavach heal merge-gate`

Fail-closed auto-merge decision for a heal PR. ALLOWS merge ONLY when all hold: the master switch is ON (env `KAVACH_HEAL_AUTOMERGE=1`, default OFF) AND CI is green AND 3-witness passed AND the diff touches NO protected path. Exit 0 = allow, non-zero = deny (and prints every failing reason). Kavach decides; it does not itself perform the merge

| Flag | Description | Default |
|---|---|---|
| `--pr` | PR number to evaluate (drives `gh pr checks` + `gh pr diff`) |  |
| `--witness-pass` | Assert the heal contract's 3-witness verification passed for this PR. Absent → treated as NOT verified (fail-closed) |  |

## `kavach heal sweep`

Proactive bug-hunt: run the repo's non-AI quality gates (cargo check, clippy -D warnings, machete) and, for each FAILING gate, capture a self-heal card so the loop fixes the defect BEFORE CI does. Idempotent per gate (re-sweep updates one card per gate). Kavach never calls an LLM

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the cards belong to |  |

## `kavach install`

Install Kavach's OFFICIAL hook config into a native tool (CC/Cursor/Codex/…)

| Flag | Description | Default |
|---|---|---|
| `--vendor` | Which tool(s): cc \| cursor \| codex \| gemini \| pi \| all |  |
| `--dry-run` | Preview the would-be action without writing any file |  |

```
EXAMPLES:
  kavach install --vendor all --dry-run
  kavach install --vendor cursor

WHEN: one-time onboarding — makes each tool load Kavach via its OWN official hook mechanism. Backs up + idempotent.
```

## `kavach lint`

Install the per-language strict-rules profile (Rust/TS/Go) so the build FAILS on bad patterns — language-agnostic, no suppression. SOURCE: decision.lint.language-profile-template

## `kavach lint audit`

Whole-repo over-engineering scan: ranked delete/stdlib/native/yagni/shrink

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root to scan (default: walk up from cwd to a manifest) |  |

## `kavach lint debt`

Harvest simplification-ceiling markers into a debt ledger

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root to scan (default: walk up from cwd to a manifest) |  |

## `kavach lint init`

Detect the stack(s) here and install the strict-rules manifest for each

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root to operate on (default: walk up from cwd to a manifest) |  |
| `--dry-run` | Report what would be written without touching any file |  |

## `kavach loop`

Manage autonomous execution loop (harness engineering)

```
See: kavach loop --help
```

## `kavach loop start`

Start an autonomous execution loop with a target condition

| Flag | Description | Default |
|---|---|---|
| `--target` | Target condition: \"phase:TEST\", \"kanban:empty\", \"goal\" |  |
| `--max` | Maximum iterations before forced stop (default: 50) | 50 |

## `kavach loop status`

Show current loop status

## `kavach loop stop`

Stop the current autonomous loop

## `kavach loophole`

Meta-Harness Loophole Loop: hunt loopholes in the SYSTEM ITSELF across the six attack lenses, record each to the Kavach DB, and capture a heal card so the loop fixes it — then re-hunt until dry. Emits a per-iteration YAML to a /tmp working dir to precisely target each round's unit of work

```
EXAMPLES:
  kavach loophole sweep --project P

WHEN: continuously self-interrogate the codebase for concurrency/failure/malformed/authz/replay/boundary loopholes. Kavach detects (non-AI) + records; the subscription agent fixes.
```

## `kavach loophole cron`

Install the PROACTIVE host schedule: a code-owned launchd `LaunchAgent` that runs `kavach loophole loop` on a daily calendar interval. The third trigger (on-demand CLI + stop-gate hook + this cron) of the meta-harness loop

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the scheduled loop hunts + records findings for |  |
| `--hour` | Hour of day (0–23, local time) the daily loop fires | 4 |
| `--dry-run` | Render the plist to stdout instead of writing it (no filesystem change) |  |

## `kavach loophole loop`

Loop-until-dry: re-run sweep rounds (each emits its own /tmp iteration YAML) until `dry_rounds` consecutive rounds surface NO new (lens, site) finding, OR `max_rounds` is hit. This is the meta-harness loop that keeps hunting + recording until the system converges. Bounded; never infinite

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the findings/cards belong to |  |
| `--run-id` | Run id grouping every round's /tmp iteration YAML | loop |
| `--dry-rounds` | Consecutive no-new-finding rounds that declare convergence (dry) | 2 |
| `--max-rounds` | Hard cap on rounds — the runaway brake (never loops forever) | 10 |

## `kavach loophole sweep`

Run ONE loophole-hunt round: emit the iteration YAML to /tmp, scan the six attack lenses over the workspace, record each finding to the DB (mistakes) and capture a heal card. Idempotent per (lens, site)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug the findings/cards belong to |  |
| `--run-id` | Sweep run id (groups iterations; defaults to a fixed `adhoc` id so a re-run is idempotent on the same findings). Override per scheduled run | adhoc |
| `--round` | 1-based round number within the run (the loop-until-dry counter) | 1 |

## `kavach mistake`

Inspect / clear the K-PRI mistake ledger (auto-populated by stop-gate behavioral blocks)

```
EXAMPLES:
  kavach mistake stats --project nicole-carpenter
  kavach mistake list --project P --limit 10 [--gate behavioral_breaker_tripped]
  kavach mistake inspect --project P --key mistake.<gate>.<slug>
  kavach mistake clear --project P --key K --confirm
  kavach mistake clear-all --project P --confirm
```

## `kavach mistake clear`

Delete one mistake row after human review (--confirm required)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Mistake row key to delete |  |
| `--confirm` | Required safety flag — refuses without it |  |

## `kavach mistake clear-all`

Wipe all mistake rows for a project (--confirm required)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--confirm` | Required safety flag — refuses without it |  |

## `kavach mistake inspect`

Show full content for one mistake row (gate block detail + metadata)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Mistake row key (from `mistake list` or stop-gate advisory) |  |

## `kavach mistake list`

List mistake ledger rows sorted by `hit_count` (most recurring first)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose mistake rows to list |  |
| `--limit` | Max rows to print (default 10) | 10 |
| `--gate` | Filter to one gate name (e.g. `behavioral_breaker_tripped`) |  |

## `kavach mistake record`

Self-file a mistake the stop-gate detectors did NOT catch. The LLM-facing door into the ledger: when you realize you erred, categorize + persist it HERE so SessionStart reinjects it next session. Writes to the shared global namespace (visible to every project). SOURCE: decision.mistake-llm-self-report

| Flag | Description | Default |
|---|---|---|
| `--gate` | Short gate/category name (e.g. `assumed_without_verifying`). Sanitized |  |
| `--banned` | The banned sample — the phrase/behavior that was the mistake |  |
| `--instead` | The correct action to take instead next time |  |

## `kavach mistake stats`

Aggregate hit counts by gate (dashboard for recurring failures)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

## `kavach oversized`

Scan for oversized files and circular dependency clusters

## `kavach oversized scan`

Scan a directory for files whose code-LOC exceeds the threshold. Uses `tokei` to count code lines (excludes comments + blanks)

| Flag | Description | Default |
|---|---|---|
| `--dir` | Root directory to scan (defaults to current working directory) | . |
| `--threshold` | Code-LOC threshold. Files at or below this are silent. 3-tier hierarchy: <=100 OK, >100 P1 advise, >200 P0 block, >500 urgent | 100 |
| `--format` | Output format | text |

## `kavach phase`

Manage SDLC development phases (PLAN/IMPLEMENT/TEST/HARDEN)

```
See: kavach phase --help
```

## `kavach phase advance`

Advance to the next phase (if Definition of Done is met)

## `kavach phase iteration-done`

Mark the current iteration file as done

## `kavach phase iteration-list`

List files completed in the current phase

## `kavach phase iteration-start`

Start an iteration. With a file path, binds that file. With NO arg, auto-pulls the next runnable DAG card from the roadmap (kanban-driven)

| Flag | Description | Default |
|---|---|---|
| `<file>` | File path to work on. Omit to auto-pull the next runnable roadmap card |  |

## `kavach phase set`

Set the current phase (admin override)

| Flag | Description | Default |
|---|---|---|
| `<phase>` | Phase name: PLAN, IMPLEMENT, TEST, or HARDEN |  |

## `kavach phase spike-end`

End spike mode

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

## `kavach phase spike-start`

Start spike mode (bypass spec gates)

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--hours` | Duration in hours |  |
| `--reason` | Spike reason |  |

## `kavach phase status`

Show current phase and iteration status

## `kavach phase tier-set`

Set project tier (refactor, feature, platform)

| Flag | Description | Default |
|---|---|---|
| `<tier>` | Target tier: refactor, feature, or platform |  |
| `--project` | Project slug |  |
| `--reason` | Change reason |  |
| `--override-flag` | Allow downgrade (otherwise promotion-only) |  |

## `kavach pipeline`

Pipeline operations: plan from `AppSpec`, show status of multi-stage workflow. SOURCE: 42-pattern catalog §5.1 Agent-Skill-Command Triad — initializer→subagent

```
EXAMPLES:
  kavach pipeline plan --project P --spec-key app_spec.build
  kavach pipeline status --project nicole-carpenter

WHEN: Bootstrap roadmap from app_spec; track multi-stage workflow counts.
```

## `kavach pipeline plan`

Read an `app_spec` entry and emit roadmap items (status=todo) from its tasks

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--spec-key` | `AppSpec` `entry_key` to read from `memory_entries` (`category=app_spec`) |  |

## `kavach pipeline status`

Show counts of {todo, `in_progress`, done, verified, blocked, deferred} for a project

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |

## `kavach rules`

Manage rules and skills

## `kavach rules check`

Check rules against a file

| Flag | Description | Default |
|---|---|---|
| `<path>` | File path to check |  |

## `kavach rules generate`

Generate rules from detected patterns

| Flag | Description | Default |
|---|---|---|
| `<dir>` | Directory to scan |  |

## `kavach rules list`

List all loaded rules/skills

## `kavach rules show`

Show a specific rule/skill

| Flag | Description | Default |
|---|---|---|
| `<name>` | Rule/skill name |  |

## `kavach schema`

Print a vendor's LIVE upstream hook-contract schema source so an operator or agent can fetch + diff the current contract (realtime drift awareness)

| Flag | Description | Default |
|---|---|---|
| `--vendor` | Which tool: cc \| cursor \| codex \| antigravity (agy) \| gemini (alias). Omit with `--all` to list every vendor |  |
| `--all` | List the schema source for every vendor |  |

```
EXAMPLES:
  kavach schema --all
  kavach schema --vendor cursor

WHEN: a native tool may have changed its hook format — reference the live schema URL instead of a frozen assumption.
```

## `kavach security`

DeepSec-style security scanner: regex pre-filter → LLM deep analysis → report

## `kavach security init`

Initialize security context (threat model, auth flows, known false positives)

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root path | . |

## `kavach security process`

LLM deep analysis on filtered files (batched, resumable)

| Flag | Description | Default |
|---|---|---|
| `--input` | Input file from scan phase (default: .kavach/security-scan.json) |  |
| `--batch-size` | Batch size for parallel processing | 5 |
| `--resume` | Resume from last checkpoint |  |

## `kavach security report`

Generate security report with git metadata and fix recommendations

| Flag | Description | Default |
|---|---|---|
| `--format` | Output format: markdown \| json | markdown |
| `--output` | Output file (default: stdout) |  |

## `kavach security scan`

Fast regex pre-filter scan for security-sensitive patterns

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root path | . |
| `--output` | Output file for filtered results (default: .kavach/security-scan.json) |  |

## `kavach servers`

Start/stop/inspect the background servers (`SurrealDB` store + web UI)

```
EXAMPLES:
  kavach servers up        # ensure DB + GUI running, print URL
  kavach servers status
  kavach servers down

WHEN: bring the GUI online, or check why pages show the offline panel.
```

## `kavach servers down`

Stop the web UI (the launchd-owned `SurrealDB` server is left running)

| Flag | Description | Default |
|---|---|---|
| `--port` | Web UI port to stop. Default 7777 | 7777 |

## `kavach servers status`

Report up/down for the `SurrealDB` store + web UI

| Flag | Description | Default |
|---|---|---|
| `--port` | Web UI port to probe. Default 7777 | 7777 |

## `kavach servers up`

Ensure the `SurrealDB` store + web UI are running; print the URL

| Flag | Description | Default |
|---|---|---|
| `--port` | Web UI port (loopback). Default 7777 (unprivileged) | 7777 |

## `kavach session`

Manage session lifecycle

## `kavach session clear-test-locks`

Clear stale test run locks (use when a test was interrupted and the lock is stuck)

## `kavach session compact`

Compact session state

## `kavach session end`

End the current session

## `kavach session end-hook`

End-hook (lifecycle cleanup)

## `kavach session init`

Initialize a new session

## `kavach session land`

Land session (finalize)

## `kavach session resume`

Resume a previous session

## `kavach session validate`

Validate current session

## `kavach spec`

Manage specification artifacts (six-file context, auto-draft)

## `kavach spec auto-draft`

Auto-draft a missing spec artifact

| Flag | Description | Default |
|---|---|---|
| `<prefix>` | Prefix to draft (e.g. spec.prd, arch.api) |  |
| `--project` | Project slug |  |

## `kavach status`

Show session status, build identity, and enforcement flags

```
EXAMPLES:
  kavach status

WHEN: session-start sanity check; confirms project slug and pending gates.
```

## `kavach tailwind-plus`

Build and query the Tailwind Plus component index

## `kavach tailwind-plus index`

Walk ~/.claude/tailwind-plus/, extract keywords, write index.json

## `kavach tasks`

Audit Claude Code's `TaskCreate` JSON store (user-global by design) and infer which project each task likely belongs to. Workaround for cross-project task-list pollution — see roadmap.unit.task-injection-project-scope

## `kavach tasks audit`

Audit Claude Code `TaskCreate` JSON store and infer per-task project holdership. Reads ~/.claude/tasks/<user>/<id>.json files and prints a table mapping each task to its likely project via keyword matching

| Flag | Description | Default |
|---|---|---|
| `--user` | Override the Claude Code user dir under ~/.claude/tasks/ (auto-detected if omitted) |  |

## `kavach team`

Team-mode: DAG-aware parallel auto-dispatch of CC Team agents over the roadmap. Independent tasks fan out; blocked tasks wait for prerequisites. SOURCE: roadmap.unit.dag-parallel-dispatch

```
EXAMPLES:
  kavach team dispatch --project nicole-carpenter --dry-run
  kavach team dispatch --project P --max-parallel 4 --spawner cc-teams

WHEN: Multi-unit parallel implementation with explicit depends_on edges.
```

## `kavach team claim-batch`

Atomically batch-claim the ready wavefront for one session (all-or-nothing over `lease.acquire_set`): no two sessions double-claim the same cards

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose roadmap DAG to schedule |  |
| `--session-id` | Session id that will hold the leases |  |

## `kavach team dispatch`

Resolve the roadmap DAG and dispatch the ready wavefront in parallel

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug whose roadmap DAG to schedule |  |
| `--max-parallel` | Concurrency cap (clamped to [1,16]); default = min(16, cores-2) |  |
| `--dry-run` | Print the topological wavefront only; spawn nothing |  |
| `--spawner` | Spawn backend: cc-teams (default) \| workflow | cc-teams |

## `kavach think`

Think-mode: hybrid keyword+graph retrieval over the kavach memory corpus. Emits cited, RRF-ranked hits as JSON; auto-files a research card when the corpus is thin for the query (gap-analysis closes the self-improving loop). SOURCE: roadmap.unit.harness.brain-os.g2-think-mode

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug (scopes the gap-file write) |  |
| `<query>` | Free-text retrieval query (BM25 over title/content + concept FTS) |  |
| `--limit` | Max fused hits to return (default: 10) | 10 |

```
EXAMPLES:
  kavach think --project kavach-rs "hybrid retrieval design"
  kavach think --project P --limit 5 "lease fencing epoch"

WHEN: recover prior decisions/research before re-researching from scratch.
```

## `kavach todos`

Sync `kavach_todo`!() macros from source to kanban

## `kavach todos sync`

Scan source for `kavach_todo`!() invocations and sync to kanban

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--path` | Project root path | . |
| `--dry-run` | Preview changes without writing |  |

## `kavach toolbelt`

Provision the Rust CLI toolbelt the gates enforce (rg, fd, bat, eza, …) via `cargo binstall` — ships *with* kavach, no per-machine setup. SOURCE: arch.decision.toolbelt-binstall-subcommand

## `kavach toolbelt install`

Fetch + install the toolbelt's prebuilt binaries into the cargo bin dir

| Flag | Description | Default |
|---|---|---|
| `--yes` | Pass `--no-confirm` to `cargo binstall` (non-interactive) |  |
| `--only` | Restrict to a comma-separated subset of bin names (e.g. `rg,fd,bat`) |  |

## `kavach toolbelt list`

List every toolbelt tool with its provider crate + upstream license

## `kavach verify`

Verify a roadmap entry: cargo check + tests, then done→verified

| Flag | Description | Default |
|---|---|---|
| `--project` | Project slug |  |
| `--key` | Roadmap entry key to verify |  |
| `--crate-name` | Crate to test (default: workspace) |  |
| `--external-verified` | Record done→verified without running cargo (deploy proven live). Requires --proof |  |
| `--proof` | Audit proof (URL/sha/receipt) persisted on the card; required with --external-verified |  |

```
EXAMPLES:
  kavach verify --project nicole-carpenter --key roadmap.unit.foo --crate-name my-crate

WHEN: After implementation is done and `db status-update … --status done`.
```

## `kavach verify-frontend`

Strict TS/frontend gate: detect biome/eslint/tsc, auto-fix safe rewrites, fail on any warning. Mirror of `kavach deploy` for non-Rust projects. Self-healing harness for production codes per CLAUDE.md §10

| Flag | Description | Default |
|---|---|---|
| `--path` | Project root (must contain biome.json / eslint.config.* / tsconfig.json) |  |
| `--skip-tests` | Skip the test runner step (still runs auto-fix + strict lint + tsc) |  |
| `--prefer` | Tool preference when multiple configs are present: auto \| biome \| eslint | auto |

## `kavach web`

Launch the HTMX web UI (server-rendered) on <http://127.0.0.1>:<port>

| Flag | Description | Default |
|---|---|---|
| `--port` | TCP port to bind on loopback (default 7777, unprivileged) | 7777 |

```
EXAMPLES:
  kavach web              # serve on :7777
  kavach web --port 8080

WHEN: browse projects/roadmap/kanban/decisions/knowledge in a browser. Reads via the surreal server; run `kavach servers up` first if pages show the offline panel.
```
